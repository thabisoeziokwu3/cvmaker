{
  "name": "@f/values",
  "description": "Get the array of values for an object.",
  "repository": "git://github.com/micro-js/values.git",
  "version": "1.2.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach-array": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
