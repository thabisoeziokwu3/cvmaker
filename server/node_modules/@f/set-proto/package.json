{
  "name": "@f/set-proto",
  "description": "Set the proto of an object.",
  "repository": "git://github.com/micro-js/set-prototype.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/extend": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
