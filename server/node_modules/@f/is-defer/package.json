{
  "name": "@f/is-defer",
  "description": "Check if value looks like a defer.",
  "repository": "git://github.com/micro-js/is-defer.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/is-promise": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
